# Налаштування Email/Password Authentication в Firebase

## Проблема
При спробі реєстрації виникає помилка: `auth/operation-not-allowed`

## Рішення

### Крок 1: Відкрийте Firebase Console
1. Перейдіть на https://console.firebase.google.com/
2. Оберіть проект **luci-f1285**

### Крок 2: Активуйте Email/Password провайдер
1. У лівому меню виберіть **Authentication** (🔐 Автентифікація)
2. Перейдіть на вкладку **Sign-in method** (Методи входу)
3. Знайдіть **Email/Password** у списку провайдерів
4. Натисніть на нього
5. **Увімкніть** перемикач "Enable" (Увімкнено)
6. Натисніть **Save** (Зберегти)

### Крок 3: Перевірка
1. Оновіть сторінку додатку
2. Спробуйте зареєструватися ще раз
3. Реєстрація має пройти успішно

## Додаткові налаштування (опціонально)

### Email enumeration protection
Рекомендується **вимкнути** "Email enumeration protection" для кращої діагностики помилок під час розробки:
1. У розділі Authentication → Settings
2. Знайдіть "User account management"
3. Вимкніть "Email enumeration protection"

### Правила безпеки Firestore
Переконайтеся що правила Firestore дозволяють запис у колекцію `users`:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Дозволити читання та запис для авторизованих користувачів
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
      allow create: if request.auth != null;
    }
    
    // Інші правила...
  }
}
```

## Перевірка стану
Запустіть у терміналі:
```bash
npm run check-firebase
```

Або відкрийте Developer Tools (F12) і подивіться на консоль - там будуть детальні повідомлення про помилки.
