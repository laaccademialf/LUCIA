# 🔧 Вирішення проблеми з API ключем Firebase

## Проблема
```
Firebase: Error (auth/api-key-not-valid.-please-pass-a-valid-api-key.)
```

## Причина
Після створення або зміни файлу `.env`, dev сервер Vite не перезавантажує змінні оточення автоматично.

## ✅ Рішення

### Варіант 1: Ручний перезапуск
1. Відкрийте термінал де запущений dev сервер
2. Натисніть `Ctrl+C` щоб зупинити сервер
3. Запустіть знову: `npm run dev`
4. Оновіть сторінку в браузері

### Варіант 2: Використання скрипта
```bash
./restart-dev.sh
```

### Варіант 3: Автоматичний перезапуск в package.json
Додайте скрипт в `package.json`:
```json
{
  "scripts": {
    "restart": "pkill -f vite && npm run dev"
  }
}
```

Потім: `npm run restart`

## 🔍 Перевірка конфігурації

Переконайтеся що `.env` файл містить всі необхідні змінні:

```env
VITE_FIREBASE_API_KEY=ваш_api_key
VITE_FIREBASE_AUTH_DOMAIN=ваш_проект.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=ваш_проект_id
VITE_FIREBASE_STORAGE_BUCKET=ваш_проект.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=ваш_sender_id
VITE_FIREBASE_APP_ID=ваш_app_id
```

**Важливо:** Всі змінні повинні починатися з префіксом `VITE_` щоб Vite міг їх завантажити!

## 📝 Корисні команди

```bash
# Перевірити чи запущений dev сервер
ps aux | grep vite

# Зупинити dev сервер
pkill -f vite

# Перевірити Firebase конфігурацію
cat .env

# Перевірити чи змінні завантажені (в браузері, F12 Console)
console.log(import.meta.env)
```

## 🚨 Типові помилки

1. **Забули префікс `VITE_`** - Vite не завантажить змінну
2. **Не перезапустили сервер** - Старі значення залишаються в пам'яті
3. **Пробіли навколо `=`** - Неправильно: `KEY = value`, Правильно: `KEY=value`
4. **Лапки в значеннях** - Зайві лапки: `KEY="value"` → `KEY=value`

## 🎯 Швидке тестування

Після перезапуску відкрийте консоль браузера (F12) і виконайте:

```javascript
console.log('API Key:', import.meta.env.VITE_FIREBASE_API_KEY);
```

Якщо виводить `undefined` - змінні не завантажені, перезапустіть сервер!
