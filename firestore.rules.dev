rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ПРАВИЛА ДЛЯ РОЗРОБКИ - Дозволяє всі операції
    // ⚠️ НЕ ВИКОРИСТОВУЙТЕ У ПРОДАКШЕНІ!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
